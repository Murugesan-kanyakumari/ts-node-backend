name: Run Latest HyperExecute Tests by Project ID

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-latest-tests:
    runs-on: ubuntu-latest
    env:
      PROJECT_ID: "01K9Q78JG0N13DTGM260ZGFJBD"     # 🔥 Your project ID
      AUTH: "Basic dGVzdGVyc3BvZGlvdHJhY2tlcjp4NXhiaHA3MHZFWGhJOWZyUHBsVElkZldIemtPZ01heklLUHg3ZEN4OGdKMU00eHVGcw=="   # 🔥 Your Base64-auth string

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install jq for JSON parsing
      run: sudo apt-get install -y jq

    - name: Get All Test Runs from Project
      id: get-tests
      run: |
        echo "🔍 Fetching test runs for project: $PROJECT_ID"

        response=$(curl --location \
          "https://test-manager-api.lambdatest.com/api/atm/v1/test-run?projectId=$PROJECT_ID" \
          --header "accept: application/json" \
          --header "Authorization: $AUTH")

        echo "API Response: $response"

        test_ids=$(echo "$response" | jq -r '.data[].id' | tr '\n' ' ')

        if [ -z "$test_ids" ] || [ "$test_ids" = "null" ]; then
          echo "❌ No test runs found in project"
          exit 1
        fi

        echo "📋 Found test run IDs: $test_ids"
        echo "test_ids=$test_ids" >> $GITHUB_OUTPUT

    - name: Run All Tests Sequentially
      env:
        TEST_IDS: ${{ steps.get-tests.outputs.test_ids }}
      run: |
        echo "🚀 Starting sequential execution of all tests..."

        IFS=' ' read -ra TEST_ID_ARRAY <<< "$TEST_IDS"

        total_tests=${#TEST_ID_ARRAY[@]}
        current_test=1

        echo "🎯 Total tests to run: $total_tests"

        for test_id in "${TEST_ID_ARRAY[@]}"; do
          if [ -n "$test_id" ] && [ "$test_id" != "null" ]; then
            echo "========================================="
            echo "🔸 Test $current_test of $total_tests: $test_id"
            echo "========================================="

            echo "⏳ Triggering HyperExecute..."
            trigger_response=$(curl --location \
              'https://test-manager-api.lambdatest.com/api/atm/v1/hyperexecute' \
              --header 'accept: application/json' \
              --header 'Content-Type: application/json' \
              --header "Authorization: $AUTH" \
              --data "{
                \"test_run_id\": \"$test_id\",
                \"concurrency\": 1
              }")

            echo "Trigger Response: $trigger_response"

            job_id=$(echo "$trigger_response" | jq -r '.job_id')

            if [ "$job_id" = "null" ] || [ -z "$job_id" ]; then
              echo "❌ Failed to trigger test: $test_id"
              current_test=$((current_test + 1))
              continue
            fi

            echo "📊 HyperExecute Job ID: $job_id"

            echo "⏳ Waiting for test completion..."
            max_attempts=60
            attempt=1
            test_completed=false

            while [ $attempt -le $max_attempts ]; do
              status_response=$(curl --location \
                "https://api.hyperexecute.cloud/v2.0/job/$job_id" \
                --header "accept: application/json" \
                --header "Authorization: $AUTH")

              status=$(echo "$status_response" | jq -r '.data.status')

              case $status in
                "completed")
                  echo "✅ Test $current_test completed!"
                  test_completed=true
                  break
                  ;;
                "failed"|"error"|"cancelled")
                  echo "❌ Test failed: $status"
                  test_completed=true
                  break
                  ;;
                *)
                  echo "⏱️ Status: $status (Attempt $attempt of $max_attempts)"
                  sleep 30
                  attempt=$((attempt + 1))
                  ;;
              esac
            done

            if [ "$test_completed" = false ]; then
              echo "🕐 Test $current_test timed out after 30 minutes"
            fi

            current_test=$((current_test + 1))
            sleep 5
          fi
        done

        echo "🎉 All tests executed!"

    - name: Final Summary
      if: always()
      run: |
        echo "========================================="
        echo "📊 LambdaTest HyperExecute Summary"
        echo "========================================="
        echo "🏢 Project ID: $PROJECT_ID"
        echo "🕒 Finished at: $(date)"
        echo "🚀 Tests executed sequentially"
        echo "📌 Check LambdaTest dashboard for details"
        echo "========================================="
